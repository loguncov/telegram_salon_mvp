# Telegram Salon MVP

Telegram Web App для управления бьюти-салоном с системой ролей (владелец, мастер, клиент).

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка .env

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_bot_token_here
WEB_APP_URL=https://loguncov.github.io/telegram_salon_mvp/
```

**Получение BOT_TOKEN:**
1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен в `.env`

**Важно:** `WEB_APP_URL` должен быть HTTPS (Telegram требует HTTPS для Web Apps).

### 3. Запуск проекта

**Быстрый запуск (рекомендуется):**

**Универсальный Python скрипт (работает на всех ОС):**
```bash
python start.py
```

**Альтернативные скрипты:**

**Windows:**
```bash
start.bat          # Batch скрипт
# или
.\start.ps1         # PowerShell скрипт
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

Скрипты автоматически:
- Создадут виртуальное окружение (если его нет)
- Установят зависимости
- Запустят Backend и Bot в отдельных окнах/процессах

**Ручной запуск:**

**Запуск Backend:**
```bash
python -m uvicorn backend:app --reload --host 0.0.0.0 --port 8000
```

**Запуск бота (в отдельном терминале):**
```bash
python bot.py
```

### 4. Использование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Нажмите кнопку "💅 Открыть приложение"
4. Web App откроется в Telegram

---

## 📋 Функционал

### Роли пользователей

- **Владелец (Owner)**: Создание салона, управление мастерами и услугами
- **Мастер (Master)**: Просмотр салона, услуг и записей
- **Клиент (Client)**: Просмотр салонов, мастеров и услуг, запись на услуги

### API Endpoints

#### Owner API
- `GET /api/owner/salon` - получить салон
- `POST /api/owner/salon` - создать салон
- `POST /api/owner/masters` - добавить мастера
- `GET /api/owner/masters` - список мастеров
- `DELETE /api/owner/masters/{id}` - удалить мастера
- `POST /api/owner/services` - добавить услугу
- `DELETE /api/owner/services/{id}` - удалить услугу

#### Client API
- `GET /api/client/salons` - список всех салонов
- `GET /api/client/salons/{id}` - информация о салоне
- `GET /api/client/salons/{id}/masters` - мастера салона
- `GET /api/client/salons/{id}/services` - услуги салона
- `POST /api/client/appointments` - создать запись
- `GET /api/client/appointments` - записи клиента

#### Master API
- `GET /api/master/salon` - салон мастера
- `GET /api/master/appointments` - записи мастера

#### User API
- `GET /api/user/role` - определить роль пользователя

#### Health
- `GET /health` - проверка работоспособности

---

## 🧪 Тестирование

### Автоматизированные тесты

```bash
# Запуск всех тестов
python -m pytest test_backend.py -v

# Запуск с подробным выводом
python -m pytest test_backend.py -v --tb=short
```

**Статус:** ✅ 29/29 тестов пройдено

### Проверка работоспособности

```bash
python check_project.py
```

Этот скрипт проверит:
- ✅ Health check endpoint
- ✅ API документацию
- ✅ API для клиентов
- ✅ Создание салона
- ✅ Добавление мастеров и услуг
- ✅ Определение роли
- ✅ Доступность Web App

### API Документация

После запуска backend откройте:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

---

## 📦 Деплой Web App на GitHub Pages

### Автоматический деплой (GitHub Actions)

Проект настроен для автоматического деплоя через GitHub Actions (см. `.github/workflows/deploy.yml`).

### Ручной деплой

1. Загрузите `index.html` в корень репозитория на GitHub
2. Перейдите в **Settings → Pages**
3. Выберите источник: **Deploy from a branch**
4. Выберите ветку (обычно `main`)
5. Выберите папку: `/ (root)`
6. Нажмите **Save**

Через несколько минут сайт будет доступен по адресу: `https://loguncov.github.io/telegram_salon_mvp/`

**Важно:** После деплоя убедитесь, что `API_BASE` в `index.html` указывает на ваш backend сервер (не localhost для продакшена).

---

## 🔧 Устранение неполадок

### Backend не запускается

1. Проверьте, что порт 8000 свободен:
   ```bash
   # Windows
   netstat -ano | findstr :8000
   
   # Linux/Mac
   lsof -i :8000
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

### Бот не запускается

1. Проверьте наличие файла `.env`
2. Проверьте, что `BOT_TOKEN` указан правильно
3. Убедитесь, что токен валидный (можно проверить через @BotFather)

### Web App не открывается

1. Убедитесь, что используете Telegram (Desktop или мобильное приложение)
2. Проверьте, что backend запущен и доступен
3. Проверьте консоль браузера (F12) на наличие ошибок
4. Убедитесь, что `WEB_APP_URL` в `.env` указывает на HTTPS URL

### Ошибка 404 при открытии Web App

1. Убедитесь, что `index.html` задеплоен на GitHub Pages
2. Проверьте URL в `.env` - должен совпадать с URL GitHub Pages
3. Обновите страницу в Telegram (закройте и откройте Web App заново)

---

## 📊 Статус проекта

- ✅ Backend API: Полностью реализован и протестирован (29/29 тестов)
- ✅ Система ролей: Реализована (owner/master/client)
- ✅ Web App UI: Реализован для всех ролей
- ✅ CRUD операции: Реализованы для мастеров и услуг
- ✅ Структура записей: Подготовлена
- ⏳ Функционал записи: Заглушка (готов к реализации)

---

## 🏗️ Архитектура

- **Backend**: FastAPI (Python) с in-memory базой данных
- **Bot**: aiogram 3 (Telegram Bot API)
- **Web App**: HTML + JavaScript (Telegram Web App SDK)
- **Тестирование**: pytest + httpx

**Примечание:** Проект использует in-memory базу данных для MVP. Данные не сохраняются между перезапусками. Для продакшена рекомендуется миграция на PostgreSQL или другую БД.

---

## 📝 Структура проекта

```
telegram_salon_mvp/
├── backend.py          # FastAPI backend
├── bot.py              # Telegram bot (aiogram)
├── index.html          # Web App UI
├── test_backend.py     # Автоматизированные тесты
├── check_project.py    # Утилита проверки работоспособности
├── requirements.txt    # Python зависимости
├── .env               # Конфигурация (не в git)
└── README.md          # Документация
```

---

## 📄 Лицензия

См. файл `LICENSE`

---

**Проект готов к использованию!** 🎉
